#include "card.hpp"
#include <array>
#include <string>

class Pack 
{
    //default ctor for testing
    // Initializes the deck in order of enum types (for testing)
    /*
    Color: Red < Green < Blue
    Number: One < Two < Three
    Shape: Oval < Diamond < Squiggle
    Shading: Solid < Striped < Open
    */
    Pack();

    //to be implemented with image recognition
    //custom ctor for reading in a board with s

};